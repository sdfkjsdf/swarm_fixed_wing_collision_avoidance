# Phase 1-H: tau_hdot step -2 m/s (level → sink)
case_id: H03
description: "tau_hdot step -2 m/s (level → sink)"
end_policy: hold_last
sequence:
  - { t_start:  0.0, t_end: 10.0, airspeed: 20.0, height_rate: -2.0, lateral_acceleration: 0.0 }
